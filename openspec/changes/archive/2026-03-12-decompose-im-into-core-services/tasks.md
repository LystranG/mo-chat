## 1. 服务拆分骨架

- [x] 1.1 盘点现有 `app`、`connection-module`、`logic-module`、`message-module`、`persistence-module` 的启动与装配职责，并标注哪些 Bean 归属 `access-gateway`、`api-service`、`message-service`、`persistence-service`
- [x] 1.2 为四个服务规划新的 Gradle 模块或独立启动入口，确保每个服务都能单独启动而不再依赖单体统一装配
- [x] 1.3 提炼共享协议与领域契约层，明确哪些类型保留在 `protocol/common`，哪些只允许服务内部使用
- [x] 1.4 调整配置结构，拆出每个服务自己的 Micronaut 配置命名空间、端口和基础依赖开关

## 2. 内部 gRPC 契约

- [x] 2.1 为 `api-service` 定义内部 gRPC 契约，至少覆盖 session 解析、私聊权限校验、群发送上下文或成员列表查询
- [x] 2.2 为 `access-gateway` 定义内部 gRPC 契约，至少覆盖定点投递、踢旧连接、查询本地连接状态
- [x] 2.3 为 `message-service` 定义内部命令契约，至少覆盖私聊发送、群聊发送、离线重放、接收确认
- [x] 2.4 生成并接入 gRPC/protobuf 代码，补齐各服务之间的客户端装配与基础连通性测试
- 完成说明（2026-03-10）：已补齐 Task 2 reviewer loop 中暴露的默认启动问题，明确 `api-service-app` 与 `message-service-app` 默认关闭 legacy inbound consumer，并完成 spec/code-quality gate 与相关文档同步。

## 3. `access-gateway` 路由与连接归属

- [x] 3.1 把 TCP 接入、握手、限流、心跳和出站写回逻辑抽离到 `access-gateway`
- 完成说明（2026-03-10）：`access-gateway-app` 已装配 `NettyChatServer`、`OutboundEventSubscriber`、`UserChannelDirectory`、Redis `EventBus` 适配、gateway 本地 drop/no-op `OfflineQueue`、TLS / heartbeat / frame / host 配置，以及基于 `api-service` gRPC stub 的 `SessionResolver` 适配器；连接层对同一 channel 的同 `sessionId` 只在首次 bind 或 sessionId 变化时做远程解析，依赖故障返回 `INTERNAL_ERROR`，bind-time 解析已移出 Netty I/O 线程并补齐有界 executor / pending backlog 边界，offline fallback 仍留待后续 `message-service` 任务。spec reviewer、code-quality reviewer 已通过，fresh 验证命令为 `./gradlew :connection-module:test :access-gateway-app:test --rerun-tasks`。
- [x] 3.2 实现 bind 成功后写入 Redis 在线路由记录的流程，记录 `gatewayPod`、`connectionId`、`sessionId`、`sessionVersion`、`routeEpoch` 和租约信息
- 完成说明（2026-03-11）：`access-gateway` 已在 bind 成功后把 `gatewayPod`、`connectionId`、`sessionId`、`sessionVersion`、`routeEpoch`、`leaseDurationSeconds`、`leaseExpiresAtEpochMilli` 原子写入 Redis 在线 route；route 持久化失败会回滚本地 ownership，而“结果不确定”的写入失败会清理本地 ownership 并关闭当前 channel 以避免 split-brain。为避免阻塞 Netty event loop，bind-time route 写入已随 async bind worker 执行；同时 `mochat.access-gateway.tcp.enabled=false` 时不再强制装配 TCP runtime bean graph。
- [x] 3.3 实现单用户单活连接语义：新 bind 覆盖旧路由并触发踢旧连接流程
- 完成说明（2026-03-11）：新 bind 现在会在 Redis 覆盖在线 route 时返回被替换旧 route 的 `gatewayPod`、`connectionId`、`sessionId`、`sessionVersion`、`routeEpoch` 元数据；`SessionBindingHandler` 在新 route 持久化成功后触发 best-effort replacement flow，同 pod 通过本地连接目录按旧 `routeEpoch` 关闭旧连接，跨 pod 则通过 `mochat.access-gateway.route.peer-targets` 解析目标地址并发起 `KickConnection` gRPC。对“写入结果不确定但随后确认新 route 已持久化成功”的 confirm-success 路径，若补读被替换旧 route 元数据失败，则降级为 `replacedRoute=null` 而不回滚本次 bind；kick 失败同样不会回滚新 bind，旧连接仍保留旧 fence，交由后续 stale-route fencing / 心跳自杀路径兜底。spec reviewer 与 code-quality reviewer 已通过；验证命令覆盖 focused suite 与 `./gradlew :connection-module:test :access-gateway-app:test --rerun-tasks` fresh 基线。
- [x] 3.4 实现心跳续租与“发现 route 已失效则自杀”的逻辑，防止旧连接继续占有在线状态
- 完成说明（2026-03-11）：`access-gateway` 现在会在 heartbeat ACK 时只为“仍持有当前 route 的已绑定连接”续租 Redis 在线 route 的 TTL 与 `leaseExpiresAtEpochMilli`；`SessionBindingHandler` 在 heartbeat 续租返回 stale 时会清理本地 ownership 并自杀关闭旧连接，在 heartbeat ACK 超时时则会先同步撤销本地 ownership 并关闭 channel，再异步 best-effort 释放旧 route。最新 reviewer loop 还补齐了两条 race 回归测试：一条确保 timeout 到达时本地 owner 可见性会先被撤销，不等待异步 Redis 清理完成；另一条确保旧 heartbeat 续租的异步 stale 结果不会误杀随后完成的新 bind。fresh 验证命令为 `./gradlew :connection-module:test :access-gateway-app:test --rerun-tasks`。
- [x] 3.5 实现 gateway drain 模式：拒绝新归属、保留存量连接、宽限期后主动断开
- 完成说明（2026-03-11）：`access-gateway` 现在支持 `mochat.access-gateway.drain.enabled` 与 `mochat.access-gateway.drain.grace-period` 配置；`SessionBindingHandler` 会在新 bind / rebind 入口以及异步 bind 的关键 fence 点拒绝 drain 期间的新 ownership，并在 route 已部分写入时沿用既有清理路径回滚。已绑定连接在 grace 期间继续服务，grace 到点后 `GatewayDrainManager` 会通过 `DRAIN_GRACE_EXPIRED_EVENT` 复用 `3.4` 的 timeout 清理路径，先撤销本地 owner 可见性并关闭 channel，再异步 best-effort 清理 Redis route。`3.5` spec reviewer 与 code-quality reviewer 已通过；fresh 验证命令为 `./gradlew :connection-module:test :access-gateway-app:test --rerun-tasks`。

## 4. `api-service` 会话与业务权威

- [x] 4.1 把登录、session 签发与 session authority 职责集中到 `api-service`
- 完成说明（2026-03-11）：`api-service` 现在通过 `SessionService` 统一负责 session 签发与 authority 解析，并把权威 session record 持久化到 Redis；record 包含 `status`、`userId`、`sessionVersion` 与过期时间，旧 session 在同用户再次登录后会被标记为 `REPLACED`，而 `ApiInternalGrpcService` 已改为基于这份 Redis-backed state 返回 `ACTIVE` / `EXPIRED` / `REPLACED` / `INVALID`。本轮 reviewer loop 进一步收紧了 `4.1` fence 语义：authority record 在 active pointer 缺失或错配时 fail-closed，`SessionBindingHandler` 对已绑定连接的后续 chat 与 heartbeat renew 都会重新回查 authority，`AccessGatewayInternalGrpcService` 的 targeted delivery 也会在本地 `sessionId` / `sessionVersion` / `routeEpoch` fence 之外再次做 authority revalidation，并在 session 不再权威有效时返回 `ROUTE_STALE`。`api-service-app` 同时补齐了 Redis / `EventBus` / `OfflineQueue` 运行时装配，使登录与 session authority 不再依赖单体占位实现。`4.1` 的 spec reviewer、code-quality reviewer 与 fresh 验证 `./gradlew :logic-module:test :api-service-app:test :connection-module:test :access-gateway-app:test --rerun-tasks` 均已通过；下一未完成任务为 `4.2`，但不得回退上述 Redis-backed authority / `sessionVersion` fence 语义。
- [x] 4.2 为 session 引入 `sessionVersion` 语义，并让内部 session 解析接口返回足够的栅栏信息
- 完成说明（2026-03-11）：Java 内部 session 解析抽象现已显式暴露 `SessionAuthority(status, sessionId, userId, sessionVersion)` 与 `SessionResolver.resolveAuthority(...)`，不再保留 `resolveUserId -> ResolvedSession(..., 0L)` 这种临时拼装退路。`SessionService` 与 `GrpcSessionResolver` 会把 authority `status` 与 `sessionVersion` 一并返回给调用方，`SessionBindingHandler` 和 `AccessGatewayInternalGrpcService` 也改为直接基于 authority 结果做 bind / chat / heartbeat renew / targeted delivery 的 stale fence，而不回退 `4.1` 已完成的 Redis-backed authority revalidation 语义。fresh 验证 `./gradlew :logic-module:test :api-service-app:test :connection-module:test :access-gateway-app:test --rerun-tasks` 已通过。
- [x] 4.3 把好友、拉黑、群成员资格等消息发送前置校验收敛成内部可调用的业务接口
- 完成说明（2026-03-11）：`api-service` 现通过 `MessageSendPolicyService` 把私聊 `ALLOWED / NOT_FRIEND / BLOCKED` 与群聊 `ALLOWED / NOT_MEMBER / GROUP_NOT_FOUND + member_uids` 这些消息发送前置校验收敛成内部业务接口，底层由 `MessageRelationshipRepository` / `JdbcMessageRelationshipRepository` 提供好友状态、群存在性、成员资格与成员列表查询。`ApiInternalGrpcService` 的 `CheckPrivateMessagingPolicy` 与 `GetGroupSendContext` 不再使用“正数 ID 即放行”的 placeholder，而是直接映射这套业务语义；本轮同时补齐了 `logic-module` 业务服务测试、`api-service-app` gRPC 语义测试与 `JdbcMessageRelationshipRepositoryIntegrationTest`，但没有提前把 `message-service` 接到这套接口上，也没有回退 `4.1 / 4.2` 已完成的 session authority / `sessionVersion` fence 语义。fresh 验证 `./gradlew :logic-module:test :api-service-app:test :connection-module:test :access-gateway-app:test --rerun-tasks` 已通过。
- [x] 4.4 保持历史查询继续归属 `api-service`，并补充它与新消息/持久化边界之间的说明和测试
- 完成说明（2026-03-11）：已补齐 dedicated runtime ownership 测试来把 history read-side 归属做实：`ApiServiceHistoryOwnershipTest` 通过 EmbeddedServer + persisted history / conversation-state stub 证明 `api-service` 会装配 `/history` 与 `/conversations/{id}/state` 入口；`MessageServiceHistoryOwnershipTest` 证明当前 `message-service` dedicated runtime 仍只承载命令侧 gRPC bean，而不会 materialize `HistoryController` / `ConversationController`；`PersistenceServiceHistoryOwnershipTest` 证明当前 `persistence-service` runtime classpath 不携带这些 read-side type。文档同步补充的是 OpenSpec / 架构边界定义：后续 `5.x / 6.x` 迁移完成后，sender ACK / online delivery 不能被解释为历史可见，history query 仍应读取持久化后的消息与会话状态，因此需要保留“实时先送达、历史稍后可见”的语义窗口；本轮没有提前声称这些写路径职责已经在当前实现里完成，也没有推进 `5.x / 6.x / 7.3`，同时没有回退 `4.1 / 4.2 / 4.3` 已完成语义。focused 验证 `./gradlew :api-service-app:test --tests com.github.lystran.mochat.apiservice.ApiServiceHistoryOwnershipTest :message-service-app:test --tests com.github.lystran.mochat.messageservice.MessageServiceHistoryOwnershipTest :persistence-service-app:test --tests com.github.lystran.mochat.persistenceservice.PersistenceServiceHistoryOwnershipTest` 已通过。

## 5. `message-service` 消息编排

- [x] 5.1 把消息摄入、幂等、`seq` 分配、`msgId` 分配和 sender ACK 逻辑迁移到 `message-service`
- 完成说明（2026-03-11）：`message-service` 的 `MessageCommandGrpcService` 已不再返回 skeleton ACK，而是把私聊/群聊命令统一落到真实 `MessageIngestService`；后者现在承担幂等命中复用、`seq` 分配、`msgId` 分配、MQ 接收成功后的 sender ACK 发布，以及私聊/群聊 payload 的命令侧重封装。`MessageServiceGrpcConnectivityTest` 已补齐 private/group 两条命令路径的连通性验证，除了 `msgId/seq` 与 duplicate 幂等外，也会解码 `payloadBase64` 断言 private `recipientUid` / `nonce` / `ciphertext` 与 group `groupId` / `text` 没有在 gRPC bridge 上丢失。spec reviewer 与 code-quality reviewer 均已通过。
- [x] 5.2 接入 `api-service` 的内部校验接口，确保私聊/群聊发送不再依赖单体内直接调用仓储
- 完成说明（2026-03-11）：`message-service` dedicated runtime 现在默认通过 `GrpcMessageSendPolicyGateway` 消费 `api-service` 的内部发送前置校验 gRPC，并用 `MessageServiceGrpcWiringTest` 证明默认装配拿到的是 gRPC policy gateway。为避免 dedicated `message-service` 悄悄回退到单体内仓储型校验，`RepositoryBackedMessageSendPolicyGateway` 已明确禁止在 `micronaut.application.name=message-service` 的 runtime materialize；同时 `api-grpc-enabled=false` 时的 wiring 测试会断言 dedicated runtime 不会 fallback 到 repository-backed policy gateway。focused 验证 `./gradlew :logic-module:test --tests com.github.lystran.mochat.logic.chat.MessageIngestServiceTest --tests com.github.lystran.mochat.logic.chat.MessageIngestServiceRelationshipAndGroupTest :message-service-app:test --tests com.github.lystran.mochat.messageservice.MessageServiceGrpcConnectivityTest --tests com.github.lystran.mochat.messageservice.GrpcMessageSendPolicyGatewayTest --tests com.github.lystran.mochat.messageservice.MessageServiceGrpcWiringTest --tests com.github.lystran.mochat.messageservice.MessageServiceHistoryOwnershipTest :api-service-app:test --tests com.github.lystran.mochat.apiservice.ApiServiceGrpcConnectivityTest --rerun-tasks` 已通过。
- [x] 5.3 接入 Redis 在线路由解析与 `access-gateway` 定点投递，统一处理 `DELIVERED`、`USER_OFFLINE`、`ROUTE_STALE`、`WRITE_FAILED`
- 完成说明（2026-03-12）：`message-service` 的 `GrpcMessageRecipientDispatcher` 已正式收口到 dedicated runtime：它会读取 Redis `online:user:{uid}` route，解析 `gatewayPod`、`connectionId`、`sessionId`、`sessionVersion`、`routeEpoch`，再通过 `gatewayPod -> targetAddress` 调用 `AccessGatewayDispatchApi.DeliverToConnection`，并把 gateway gRPC 结果统一映射为 `DELIVERED`、`USER_OFFLINE`、`ROUTE_STALE`、`WRITE_FAILED`。本轮补齐了 focused coverage：`GrpcMessageRecipientDispatcherTest` 现覆盖 private/group dispatch、gateway 返回 `USER_OFFLINE` / `WRITE_FAILED`、缺失 targetAddress 映射，以及 gateway RPC 直接失败时归一化为 `WRITE_FAILED`；`MessageServiceGrpcWiringTest` 也证明了 dedicated runtime 在 Redis + gateway dispatch bean 可用时确实装配 `GrpcMessageRecipientDispatcher`。本轮刻意未推进 `5.4` 的 route refresh once / offline queue fallback，也未触碰 `4.1`~`4.4` 已完成边界。fresh focused 验证 `./gradlew :message-service-app:test --tests com.github.lystran.mochat.messageservice.GrpcMessageRecipientDispatcherTest --tests com.github.lystran.mochat.messageservice.MessageServiceGrpcWiringTest :access-gateway-app:test --tests com.github.lystran.mochat.accessgateway.AccessGatewayGrpcConnectivityTest --rerun-tasks` 与更稳妥验证 `./gradlew :logic-module:test --tests com.github.lystran.mochat.logic.chat.MessageIngestServiceTest --tests com.github.lystran.mochat.logic.chat.MessageIngestServiceRelationshipAndGroupTest :message-service-app:test --tests com.github.lystran.mochat.messageservice.MessageServiceGrpcConnectivityTest --tests com.github.lystran.mochat.messageservice.GrpcMessageSendPolicyGatewayTest --tests com.github.lystran.mochat.messageservice.GrpcMessageRecipientDispatcherTest --tests com.github.lystran.mochat.messageservice.MessageServiceGrpcWiringTest :access-gateway-app:test --tests com.github.lystran.mochat.accessgateway.AccessGatewayGrpcConnectivityTest --rerun-tasks` 均已通过。
- [x] 5.4 实现“重查路由一次，仍失败则写离线队列”的降级策略
- 完成说明（2026-03-12）：`MessageIngestService` 现在会在 `message-service` 编排层消费 `MessageRecipientDispatcher` 的四态结果：private path 在首次 `USER_OFFLINE` / `ROUTE_STALE` / `WRITE_FAILED` 或 runtime 异常后会再做一次 fresh dispatch，二次仍失败才把 replayable delivery envelope 写入 `OfflineQueue`；group path 则只对首次失败的 recipient 做二次 fresh dispatch，并且只把二次仍失败的 recipient 入队。为保持 `5.3` 的职责边界，本轮没有把 refresh 逻辑塞回 `GrpcMessageRecipientDispatcher`，而是复用了它“每次调用 fresh 读 Redis route”的语义；同时 dedicated `message-service` runtime 已补齐 `RedisOfflineQueue` 装配，并在无 Redis bean 的测试图里提供 no-op fallback，避免影响现有 gRPC connectivity coverage。新增 focused 红绿测试覆盖 private retry-success / retry-fail / retry-exception、group selective enqueue，以及 dedicated runtime 的 `OfflineQueue` wiring；本轮没有推进 `5.5` 的 `ReplayOfflineMessages`、sender ACK 终态收口或 offline replay。fresh focused 验证 `./gradlew :logic-module:test --tests com.github.lystran.mochat.logic.chat.MessageIngestServiceTest --tests com.github.lystran.mochat.logic.chat.MessageIngestServiceRelationshipAndGroupTest :message-service-app:test --tests com.github.lystran.mochat.messageservice.GrpcMessageRecipientDispatcherTest --tests com.github.lystran.mochat.messageservice.MessageServiceGrpcWiringTest :access-gateway-app:test --tests com.github.lystran.mochat.accessgateway.AccessGatewayGrpcConnectivityTest --rerun-tasks` 与更稳妥验证 `./gradlew :logic-module:test --tests com.github.lystran.mochat.logic.chat.MessageIngestServiceTest --tests com.github.lystran.mochat.logic.chat.MessageIngestServiceRelationshipAndGroupTest --tests com.github.lystran.mochat.logic.chat.OfflineReplayServiceTest :message-service-app:test --tests com.github.lystran.mochat.messageservice.MessageServiceGrpcConnectivityTest --tests com.github.lystran.mochat.messageservice.GrpcMessageSendPolicyGatewayTest --tests com.github.lystran.mochat.messageservice.GrpcMessageRecipientDispatcherTest --tests com.github.lystran.mochat.messageservice.MessageServiceGrpcWiringTest :access-gateway-app:test --tests com.github.lystran.mochat.accessgateway.AccessGatewayGrpcConnectivityTest --rerun-tasks` 均已通过。
- [x] 5.5 调整 sender ACK 语义与离线重放逻辑，确保 `SEND_ACK` 只表示 MQ 接收成功，不表示 DB 已提交
- 完成说明（2026-03-12）：`SEND_ACK` 语义保持收敛在 `MessageIngestService` 的 MQ publish success 之后：本轮没有把 success ACK 绑定到 DB commit，也没有提前推进 `6.x` 的持久化拆分；既有 focused 测试继续证明 publish 失败或抛异常时不会发 success ACK。`message-service` 侧的 `ReplayOfflineMessages` 已不再返回 skeleton，而是通过 `MessageServiceOfflineReplayService` 实际 `drain OfflineQueue`、消费 `5.4` 已落地的 replayable `ChatMessageDelivery` envelope，并在 dispatch 失败时把当前及剩余 payload 重新入队，避免丢失。登录后的 replay 触发则通过新的 `LoginOfflineReplayGateway` 收口：dedicated `api-service` runtime 在存在 `message-service` gRPC stub 时优先走 `GrpcLoginOfflineReplayGateway` 调内部 `ReplayOfflineMessages` 命令；未接 message-service stub 的现有 logic/http 测试图仍可通过本地 fallback `LocalLoginOfflineReplayGateway` 复用 legacy `OfflineReplayService`，因此没有破坏原有测试基线。fresh focused 验证已通过：`./gradlew :message-service-app:test --tests com.github.lystran.mochat.messageservice.MessageServiceGrpcConnectivityTest --rerun-tasks`、`./gradlew :logic-module:test --tests com.github.lystran.mochat.logic.http.AuthControllerTest --tests com.github.lystran.mochat.logic.http.AuthControllerOfflineReplayHttpTest --rerun-tasks`、`./gradlew :api-service-app:test --tests com.github.lystran.mochat.apiservice.ApiServiceGrpcWiringTest --rerun-tasks`；更稳妥的回归验证 `./gradlew :logic-module:test --tests com.github.lystran.mochat.logic.chat.MessageIngestServiceTest --tests com.github.lystran.mochat.logic.chat.MessageIngestServiceRelationshipAndGroupTest --tests com.github.lystran.mochat.logic.chat.OfflineReplayServiceTest --tests com.github.lystran.mochat.logic.http.AuthControllerTest --tests com.github.lystran.mochat.logic.http.AuthControllerOfflineReplayHttpTest :message-service-app:test --tests com.github.lystran.mochat.messageservice.MessageServiceGrpcConnectivityTest --tests com.github.lystran.mochat.messageservice.MessageServiceGrpcWiringTest :api-service-app:test --tests com.github.lystran.mochat.apiservice.ApiServiceGrpcWiringTest --rerun-tasks` 也已通过。

## 6. `persistence-service` 持久化边界

- [x] 6.1 把 MQ consumer 和持久化事务链路迁移到 `persistence-service`
- 完成说明（2026-03-12）：`persistence-service-app` 现在已补齐 dedicated `PersistenceServiceRuntimeFactory` / `PersistenceServiceRuntimeLifecycle`，会在自己的 runtime 内装配 `MessageRepository`、`ConversationRepository`、`GroupMessageCache`、`MqConsumer`、`DefaultMQPushConsumer` 与 `RocketMqPersistenceConsumer`，并用 `mochat.persistence-service.queue.consumer-enabled` 控制 MQ consumer 启停；`PersistenceServiceApplicationContextTest` 同时覆盖了 dedicated 配置命名空间、基于 dedicated app 默认配置的 `DataSource` / `Flyway` / `DefaultMQPushConsumer` materialization，以及“只给 Redis 最小 test bridge 时仍可装出完整 consumer bean graph”的 focused 场景。legacy monolith `app` 的兼容壳保持不动，`SEND_ACK` / replay / history read-side 边界没有回退。fresh 验证已通过：`./gradlew :persistence-module:test --tests com.github.lystran.mochat.persistence.RocketMqPersistenceConsumerTest --tests com.github.lystran.mochat.persistence.MqConsumerTransactionTest --tests com.github.lystran.mochat.persistence.TransactionalPersistenceTest :persistence-service-app:test --tests com.github.lystran.mochat.persistenceservice.PersistenceServiceApplicationContextTest --tests com.github.lystran.mochat.persistenceservice.PersistenceServiceHistoryOwnershipTest :app:test --tests com.github.lystran.mochat.runtime.PersistenceRuntimeLifecycleTest --tests com.github.lystran.mochat.AppRuntimeAssemblyTest --rerun-tasks`。
- [x] 6.2 让 `persistence-service` 独占 `messages`、`conversations`、receipt/state 推进和群缓存 post-commit 更新
- 完成说明（2026-03-12）：`ReceiptConversationStateStore` 契约已迁到 `message-module` 作为共享写侧契约，默认 JDBC owner `JdbcReceiptConversationStateStore` 已迁到 `persistence-module` 并由 dedicated `persistence-service` runtime 与 `MessageRepository`、`ConversationRepository`、`GroupMessageCache`、`MqConsumer`、`RocketMqPersistenceConsumer` 一起 materialize，因此 `messages` / `conversations` / receipt-state 推进 / 群缓存 post-commit 更新的默认 ownership 已收口到 `persistence-service`。对应地，`ApiServiceApplicationContextTest` 与 `MessageServiceGrpcWiringTest` 证明 dedicated `api-service` / `message-service` 不再 materialize receipt/state fallback owner；legacy monolith `app` 兼容壳则改为显式 opt-in，默认 `mochat.legacy.persistence.enabled=false` 且 `mochat.message-service.inbound-consumer.enabled=false`，只有在兼容模式下才恢复 `InboundMessageConsumer -> ReceiptService -> JdbcReceiptConversationStateStore` 依赖链，因此默认装配不再把 monolith 误认为 persistence source of truth。本轮只完成 ownership / wiring 收口，没有推进 `6.3` 的重复消费幂等重试验证，也没有推进 `6.4` 的“实时先送达、历史稍后可见”最终一致性验证。fresh 验证已通过：`./gradlew :persistence-module:test --tests com.github.lystran.mochat.persistence.RocketMqPersistenceConsumerTest --tests com.github.lystran.mochat.persistence.MqConsumerTransactionTest --tests com.github.lystran.mochat.persistence.TransactionalPersistenceTest :persistence-service-app:test --tests com.github.lystran.mochat.persistenceservice.PersistenceServiceApplicationContextTest --tests com.github.lystran.mochat.persistenceservice.PersistenceServiceHistoryOwnershipTest :app:test --tests com.github.lystran.mochat.runtime.PersistenceRuntimeLifecycleTest --tests com.github.lystran.mochat.AppRuntimeAssemblyTest --rerun-tasks` 与 `./gradlew :logic-module:test --tests com.github.lystran.mochat.logic.chat.JdbcReceiptConversationStateStoreTest --tests com.github.lystran.mochat.logic.chat.ReceiptServiceTest :api-service-app:test --tests com.github.lystran.mochat.apiservice.ApiServiceApplicationContextTest :message-service-app:test --tests com.github.lystran.mochat.messageservice.MessageServiceGrpcWiringTest --rerun-tasks`。
- [x] 6.3 校验 MQ 重试与数据库幂等行为，确保重复消费不会生成重复消息事实
- 完成说明（2026-03-12）：`MessageRepository` 现在会在 `messages` 唯一键冲突后按 `msg_id` 回读现有 durable fact，仅在字段完全一致时判定为 `DURABLE_DUPLICATE`；`MqConsumer` 对这类重复消费按成功 no-op 处理，不再二次推进 `conversations.latest_seq/latest_message_time`，也不重复触发 group cache。`RocketMqPersistenceConsumer` 继续把真正的 durable duplicate 解释成成功，而把 `DurableMessageConflictException` 与其他持久化失败保留在现有 retry 语义里，因此 `6.3` 只收口 MQ retry / DB 幂等边界，没有提前推进 `6.4` 的最终一致性窗口。fresh focused 验证已通过：`./gradlew :persistence-module:test --tests com.github.lystran.mochat.persistence.TransactionalPersistenceTest --tests com.github.lystran.mochat.persistence.MqConsumerTransactionTest --tests com.github.lystran.mochat.persistence.MqConsumerTransactionDockerTest --tests com.github.lystran.mochat.persistence.RocketMqPersistenceConsumerTest --tests com.github.lystran.mochat.persistence.RocketMqPersistenceConsumerDockerTest :persistence-service-app:test --tests com.github.lystran.mochat.persistenceservice.PersistenceServiceApplicationContextTest --tests com.github.lystran.mochat.persistenceservice.PersistenceServiceHistoryOwnershipTest --rerun-tasks`。
- [x] 6.4 补充“实时投递先完成、历史稍后可见”的最终一致性验证用例
- 完成说明（2026-03-12）：`ApiServiceHistoryOwnershipTest` 现通过 commit-aware read model + EmbeddedServer focused 用例把最终一致性窗口做实：同一条已发生 realtime delivery 的消息在持久化提交前不会出现在 `api-service` 的 `/history` 与 `/conversations/{id}/state` 读侧，模拟 commit 后两者才一起可见。`MessageServiceGrpcConnectivityTest` 同时补齐了 private send focused 断言，证明 `message-service` 在返回 accepted `msgId/seq` 后已完成 realtime private dispatch 尝试，而 `MessageServiceHistoryOwnershipTest` / `PersistenceServiceHistoryOwnershipTest` 继续钉住 history read-side 不会回退 materialize 到写侧 runtime。fresh focused 验证 `./gradlew :api-service-app:test --tests com.github.lystran.mochat.apiservice.ApiServiceHistoryOwnershipTest :message-service-app:test --tests com.github.lystran.mochat.messageservice.MessageServiceGrpcConnectivityTest --tests com.github.lystran.mochat.messageservice.MessageServiceHistoryOwnershipTest :persistence-service-app:test --tests com.github.lystran.mochat.persistenceservice.PersistenceServiceHistoryOwnershipTest --rerun-tasks` 已通过。

## 7. 集成验证与回滚准备

- [x] 7.1 补充跨 pod 场景集成测试，覆盖 A/B 两个用户落在不同 gateway 上的私聊投递
- 完成说明（2026-03-12）：新增 `MessageServiceCrossGatewayRoutingIntegrationTest`，用手工组装的 `message-service` + 双 `access-gateway` harness 覆盖 A/B 用户分属不同 gateway 的私聊投递路径，直接证明 `message-service` 会按 Redis route 选中拥有者 gateway，并且只有目标 gateway 上的收件连接收到 `ChatMessageDelivery` frame。为使该集成测试落到真实缺口，本轮同时把测试夹具从 Micronaut `ApplicationContext` 去耦，并补齐 `AccessGatewayInternalGrpcService.deliverToConnection(...)` 的本地 channel 写帧实现。fresh focused 验证已通过：`./gradlew :message-service-app:test --tests com.github.lystran.mochat.messageservice.MessageServiceCrossGatewayRoutingIntegrationTest :access-gateway-app:test --tests com.github.lystran.mochat.accessgateway.AccessGatewayGrpcConnectivityTest --rerun-tasks`。
- [x] 7.2 补充重复登录替换旧连接、route stale、自杀关闭和离线补推回退的测试
- 完成说明（2026-03-12）：在已有 `newerBindTriggersKickFlowForPreviousLocalConnectionAndPersistsNewActiveRoute()`、`staleRouteObservedDuringHeartbeatSelfKillsReplacedConnection()`、`replayOfflineMessagesReEnqueuesCurrentAndRemainingPayloadsWhenDispatchFails()` 的基础上，本轮新增 `AccessGatewayOnlineRouteBindingTest.newerBindOnOtherGatewayLeavesOldOwnerAliveUntilHeartbeatThenSelfKills()` 与 `MessageServiceCrossGatewayRoutingIntegrationTest.staleRouteAfterSingleRefreshFallsBackToOfflineQueue()`，把“跨 gateway 重复登录导致旧 owner 仅在下一次 heartbeat 才自杀关闭”以及“message-service 遇到 stale route 后只刷新一次，仍失败就写 replayable offline envelope”的集成证据补齐。两条新测已由主会话 fresh 复跑通过：`./gradlew :message-service-app:test --tests 'com.github.lystran.mochat.messageservice.MessageServiceCrossGatewayRoutingIntegrationTest.staleRouteAfterSingleRefreshFallsBackToOfflineQueue' --rerun-tasks` 与 `./gradlew :access-gateway-app:test --tests 'com.github.lystran.mochat.accessgateway.runtime.AccessGatewayOnlineRouteBindingTest.newerBindOnOtherGatewayLeavesOldOwnerAliveUntilHeartbeatThenSelfKills' --rerun-tasks`。
- [x] 7.3 补充 gateway drain / rollout 场景测试，验证不接新连接且存量连接能在宽限期后重连恢复
- 完成说明（2026-03-12）：在已有 `drainingGatewayRejectsNewBindOwnership()` 与 `drainGraceExpirationClosesExistingBoundConnectionAndClearsRoute()` 的基础上，本轮新增 `AccessGatewayOnlineRouteBindingTest.drainingGatewayRejectsNewBindButAllowsReconnectOnOtherGatewayAfterGrace()`，把 rollout/drain 语义补成完整链路：`gateway-a` 进入 drain 后拒绝新的 bind ownership、旧连接在 grace 结束前继续存活、grace 结束后旧 route 被清理并关闭连接、同一 session 随后可在 `gateway-b` 上重新 bind 成为新的 active route。fresh focused 验证已通过：`./gradlew :access-gateway-app:test --tests com.github.lystran.mochat.accessgateway.runtime.AccessGatewayOnlineRouteBindingTest.drainingGatewayRejectsNewBindOwnership --tests com.github.lystran.mochat.accessgateway.runtime.AccessGatewayOnlineRouteBindingTest.drainGraceExpirationClosesExistingBoundConnectionAndClearsRoute --tests com.github.lystran.mochat.accessgateway.runtime.AccessGatewayOnlineRouteBindingTest.drainingGatewayRejectsNewBindButAllowsReconnectOnOtherGatewayAfterGrace --rerun-tasks`。补充说明：整类回归 `./gradlew :access-gateway-app:test --tests com.github.lystran.mochat.accessgateway.runtime.AccessGatewayOnlineRouteBindingTest --rerun-tasks` 在当前环境可复现 Gradle `SerializableTestResultStore` 的 `NoSuchFileException`，但不是业务断言失败，因此本轮以三条直接对应需求的 focused 证据作为完成依据。
- [x] 7.4 更新运行文档与部署说明，描述四服务拓扑、共享基础设施、内部 gRPC 端口与回滚方式
- 完成说明（2026-03-12）：`docs/runbook.md` 已合并重复的 dedicated topology 章节，统一了本地双 gateway 示例中的 `gateway-b` gRPC 端口为 `19094`，并把四服务拓扑、共享 PostgreSQL / Redis / RocketMQ 基础设施、跨服务 gRPC 地址约定、route target / peer target 配置，以及回滚到 legacy compatibility shell 的步骤整理为一份单一入口说明。`docs/architecture/decompose-im-into-core-services-skeleton.md` 中已有的 Task 7 端口与 rollback baseline 继续保留，runbook 不再与之冲突。
- [x] 7.5 清理旧的单体装配假设，确认所有服务都能独立启动并通过现有或新增验证基线
- 完成说明（2026-03-12）：本轮没有再扩默认 monolith ownership，而是用 fresh focused baseline 重新证明 dedicated services 与 legacy compatibility shell 的边界已经收敛：`AccessGatewayApplicationContextTest` / `AccessGatewayGrpcWiringTest`、`ApiServiceApplicationContextTest` / `ApiServiceGrpcWiringTest`、`MessageServiceApplicationContextTest` / `MessageServiceGrpcWiringTest`、`PersistenceServiceApplicationContextTest` 以及 `AppRuntimeAssemblyTest` 均 fresh 通过。该基线同时证明 `app` 默认配置不再 materialize persistence owners，而 dedicated `access-gateway` / `api-service` / `message-service` / `persistence-service` 都能在各自 runtime 内独立装配所需 bean graph；legacy monolith 行为仅在显式打开兼容开关时恢复。fresh 验证命令为 `./gradlew :access-gateway-app:test --tests com.github.lystran.mochat.accessgateway.AccessGatewayApplicationContextTest --tests com.github.lystran.mochat.accessgateway.AccessGatewayGrpcWiringTest :api-service-app:test --tests com.github.lystran.mochat.apiservice.ApiServiceApplicationContextTest --tests com.github.lystran.mochat.apiservice.ApiServiceGrpcWiringTest :message-service-app:test --tests com.github.lystran.mochat.messageservice.MessageServiceApplicationContextTest --tests com.github.lystran.mochat.messageservice.MessageServiceGrpcWiringTest :persistence-service-app:test --tests com.github.lystran.mochat.persistenceservice.PersistenceServiceApplicationContextTest :app:test --tests com.github.lystran.mochat.AppRuntimeAssemblyTest --rerun-tasks`。
